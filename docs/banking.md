# Banking and reconciliation

Statements are parsed into fixed-precision transactions with page and raw evidence anchors. Folder
codes add weak account metadata; document content wins conflicts. Reconciliation is deterministic and
returns component scores, reasons and conflicts. Ambiguous/conflicting candidates require a person.

